<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="com.xin.dao.UserMapper">
  <select id="getAllUsers" resultType="user">
    select * from user;
  </select>
  
  <select id="getUserById" resultType="com.xin.beans.User">
  	select * from user where id = #{id};
  </select>
  
  <select id="getUserByParam" resultType="com.xin.beans.User"> 
  	select * from user where id = #{id} and name = #{name};
  </select>
  
  <insert id="create" parameterType="com.xin.beans.User">
  	insert into user (id,name,pwd) values (#{id},#{name},#{pwd});
  </insert>
  
  <update id="update" parameterType="com.xin.beans.User">
  	update user set name=#{name},pwd=#{pwd} where id = #{id};
  </update>
  
  <delete id="deleteById">
  	delete from user where id = #{id};
  </delete>
  
</mapper>
